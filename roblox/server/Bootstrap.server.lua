-- ModuleScripts do not execute automatically. Keep this script as the only
-- runtime entry point; all tracking behavior belongs in MainModule.
require(script.Parent:WaitForChild("MainModule")).start()
